extends layout
block content
    div 
        a(href=`/users/insert`) Aggiungi
    .container
        each unit in result
            div(class=`${unit.Rarity}`)
                a(href=`/users/dettagli/${unit.Unit}`) #{unit.Unit} 
                    img(src=`/images/${unit.Unit}.png`, height="450" weight="700")
